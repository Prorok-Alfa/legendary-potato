#include <iostream>
 
using namespace std;
 
int main()
{
    char s;
    while (cin >> s) {
        s = tolower(s);
        if (!(s == 'a' || s == 'o' || s == 'e' || s == 'u' || s == 'i' || s == 'y'))
            cout << '.' << s;
    }
    return 0;
}
